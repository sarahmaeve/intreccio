[package]
name = "weave"
version = "0.1.0"
edition = "2021"
description = "HTML parsing and transformation for triglot weave lessons"

[dependencies]
scraper = "0.23"
ego-tree = "0.10"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
