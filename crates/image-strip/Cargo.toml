[package]
name = "image-strip"
version = "0.1.0"
edition = "2021"
description = "Strip EXIF/XMP/IPTC metadata from JPEG and PNG images while preserving orientation"

[dependencies]
img-parts = "0.3"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
